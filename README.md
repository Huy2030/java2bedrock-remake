This project is a remake based on the original repository at https://github.com/Kas-tle/java2bedrock.sh, featuring improvements to certain functions and the removal of unnecessary ones, as follows:

- Fixed an issue related to generating UV textures for 3D models
- Automatically generates `item_texture.json` paths for 3D models (you still need to create the icon for the 3D model yourself using BlockBench)
- Delete unnecessary junk filess